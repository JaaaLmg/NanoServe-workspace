# AGENTS.md — NanoServe 智能体开发规则

本文件供在本仓库工作的智能体（Agent）长期参考和遵循。目标：在多轮、多智能体的迭代中保持代码质量、文档一致性和分支秩序。开始任何任务前，先通读本文件。

## 1. 项目背景

- NanoServe 是基于 [nano-vLLM](./docs/nanovllm-README.md) 逐步演进的推理服务项目，按 [`plan.md`](./plan.md) 的 20 天计划推进（Day 1–13 为阶段一/二，后续为服务化）。
- 每个开发日对应一篇设计文档（`docs/` 目录），文档中包含**目标、非目标、实现步骤、风险点和最终验收清单**。设计文档是开发的唯一权威需求来源。

## 2. 分支模型（必须遵守）

```
main        稳定基线，不直接开发
 └─ dev     集成分支，从 main 迁出
     └─ feature/<功能名>   功能分支，从 dev 迁出，体现所实现的功能
```

- 开始功能开发前：从当前 `main` 确保存在 `dev` 分支；从 `dev` 迁出 `feature/<功能名>` 分支（英文命名，用连字符，如 `feature/request-lifecycle-state-machine`）。
- 一个功能一个分支；不要在 `main` 或 `dev` 上直接提交功能代码。
- **未经用户明确许可，不要合并分支、不要 push、不要删除分支、不要改变既有分支状态。**
- 若用户明确要求"完成后不要提交"，则将所有改动留在工作区，由用户审阅后自行提交。

## 3. 开发流程

1. **读文档**：找到当前阶段对应的设计文档（`docs/README.md` 有索引），完整阅读目标、非目标、迁移图/接口约定和验收清单。
2. **实现**：按文档的"实现步骤"与"模块调整方案"开发；遇到文档未覆盖的设计决策，优先选择与既有代码风格和不变量一致的方案，并在代码注释与验收记录中说明取舍。
3. **测试**：先写不依赖 GPU/模型权重的纯 CPU 单元测试（Scheduler、BlockManager、状态机等均为纯 CPU 逻辑）；文档若要求 GPU 最小验证且环境具备（GPU + 权重），再补充真实运行验证。
4. **回归**：提交前运行全量 `python -m pytest -q`，确保不破坏既有天数的行为。
5. **文档**：
   - 新增设计文档必须加入 `docs/README.md` 索引；
   - 完成后新增/更新对应的验收记录（`docs/dayN-validation.md`），**必须注明 GPU/模型是否可用、实际执行的命令与结果、未覆盖的边界**；
   - **不得修改设计文档中的验收清单**——清单是需求的一部分，完成情况记录在验收记录文档中。

## 4. 代码规范

- **关键代码必须有中文注释**：状态机规则、资源记账、并发/序列化协议、非直观的设计取舍，都要有解释"为什么"的中文注释，保证后人可读。简单的自解释代码不必强行注释。
- 遵循上游 nano-vLLM 的简洁风格：不引入不必要的抽象层，模块职责与 `docs/architecture.md` 保持一致。
- **核心规则集中定义，单一权威**：例如请求状态机的合法迁移表只允许存在于 `Sequence`（`VALID_TRANSITIONS`）一处，Scheduler 等调用方必须通过统一迁移入口修改状态，不得散落直接赋值。
- 破坏性操作（释放 KV block、清队列）必须幂等；非法操作尽早抛显式异常（如 `InvalidStateTransition`），不要悄悄吞掉。
- 涉及张量并行 pickle 协议（`Sequence.__getstate__/__setstate__`）的改动，必须保持版本化并兼容旧格式，配套序列化单测。

## 5. 测试规范

- 测试文件放 `tests/`，命名 `test_<主题>.py`；文件头用 docstring 说明覆盖范围、是否依赖 GPU/模型、重复执行命令。
- 不加载模型：用 `SimpleNamespace` 构造 Config 桩、`Sequence.__new__` 等手段绕开 GPU 初始化；测试内手动对齐 `Sequence.block_size`。
- 采样 token 由测试直接注入（`postprocess` 参数），不依赖真实模型输出。
- **严禁把未实际执行的测试写成通过**：GPU 路径未跑就在验收记录里写明"未实测"。
- 全量回归命令：`python -m pytest -q`。

## 6. 提交与沟通

- 提交说明需概括：功能变更、资源/兼容性影响、测试情况。
- 完成任务后向用户报告：实现了什么、测试结果（含数字）、哪些验收项通过、哪些边界未覆盖、当前分支状态与工作区改动清单。
- 对文档没写清楚的设计分歧，先给出与现有不变量一致的默认实现并明确标注，不要中断等待澄清（除非是需求级的方向问题）。
